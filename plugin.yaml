# Plugin Definition below. This is essentially a valid helm values file that will be merged
# with the other vcluster values during vcluster create or helm install.
plugin:
  container-resources-sync:
    #image: gcr.io/spectro-images-public/release/vcluster-container-resource-upsync-plugin:0.0.1
    image: tylergillson/vcluster-container-resource-upsync-plugin:latest
    imagePullPolicy: IfNotPresent